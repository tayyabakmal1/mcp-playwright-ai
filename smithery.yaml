# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    properties: {}
  # Direct command specification for Smithery (preferred over inline functions)
  command: node
  args:
    - dist/index.js
  env: {}